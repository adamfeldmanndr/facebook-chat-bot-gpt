{
  "name": "chat-bot-gpt",
  "version": "1.0.0",
  "main": "app.ts",
  "scripts": {
    "serve": "cd build && node app.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon src/app.ts",
    "build": "rimraf ./build && tsc && cp .env ./build/.env && cp cookies.json ./build/cookies.json"
  },
  "keywords": ["chat gpt", "chat bot"],
  "author": "Hiep Nguyen",
  "license": "MIT",
  "dependencies": {
    "chatgpt": "^4.8.3",
    "dotenv": "^16.0.3",
    "facebook-chat-api": "^1.8.0",
    "winston": "^3.8.2"
  },
  "devDependencies": {
    "@types/node": "^18.14.2",
    "@types/winston": "^2.4.4",
    "nodemon": "^2.0.20",
    "rimraf": "^4.2.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
